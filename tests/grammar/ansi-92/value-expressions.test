# unsigned literal number value expression primary
>SELECT 1 FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: literal[1]
   referenced tables:
     0: t1>
# character string literal value expression primary
>SELECT 'a' FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: literal['a']
   referenced tables:
     0: t1>
# national character string literal value expression primary
>SELECT N'motorček' FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: literal['motorček']
   referenced tables:
     0: t1>
# bit string literal value expression primary
>SELECT B'01000101101' FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: literal['01000101101']
   referenced tables:
     0: t1>
# hex string literal value expression primary
>SELECT X'FE1CD34A' FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: literal['FE1CD34A']
   referenced tables:
     0: t1>
# General value expression primaries
>SELECT USER, CURRENT_USER, SESSION_USER, SYSTEM_USER, VALUE FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: USER
     1: CURRENT_USER
     2: SESSION_USER
     3: SYSTEM_USER
     4: VALUE
   referenced tables:
     0: t1>
# COUNT set function specifications
>SELECT COUNT(*), COUNT(DISTINCT a), COUNT(a) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: COUNT(*)
     1: COUNT(DISTINCT column-reference[a])
     2: COUNT(column-reference[a])
   referenced tables:
     0: t1>
# SUM set function specifications
>SELECT SUM(DISTINCT a), SUM(a) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: SUM(DISTINCT column-reference[a])
     1: SUM(column-reference[a])
   referenced tables:
     0: t1>
# AVG set function specifications
>SELECT AVG(DISTINCT a), AVG(a) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: AVG(DISTINCT column-reference[a])
     1: AVG(column-reference[a])
   referenced tables:
     0: t1>
# MIN set function specifications
>SELECT MIN(DISTINCT a), MIN(a) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: MIN(DISTINCT column-reference[a])
     1: MIN(column-reference[a])
   referenced tables:
     0: t1>
# MAX set function specifications
>SELECT MAX(DISTINCT a), MAX(a) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: MAX(DISTINCT column-reference[a])
     1: MAX(column-reference[a])
   referenced tables:
     0: t1>
# Nested set function specifications
>SELECT MAX(COUNT(a)) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: MAX(COUNT(column-reference[a]))
   referenced tables:
     0: t1>
# A simple scalar subquery
>SELECT (SELECT b FROM t2) FROM t1
OK
statements[0]:
  <statement: SELECT
   selected columns:
     0: scalar-subquery[
<statement: SELECT
   selected columns:
     0: column-reference[b]
   referenced tables:
     0: t2>
]
   referenced tables:
     0: t1>

